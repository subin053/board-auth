<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.green.boardauth.application.board.BoardMapper">
    <insert id="save" useGeneratedKeys="true" keyProperty="id"><!--1바로 pk가져오겠다 pk의 값의 이름-->
        INSERT INTO board
           SET title = #{title}
             , contents = #{contents}
             , user_id = #{userId}
    </insert>

    <select id="findAll">
        SELECT b.id, b.title, b.created_at, u.nm
        FROM board b
        INNER JOIN user u
        ON u.id = b.user_id
        <if test=" searchText != null and searchText != '' ">
            WHERE b.title LIKE '%${searchText}%'
        </if>
        ORDER BY b.id DESC
        LIMIT #{startIdx}, #{size}
    </select>

    <select id="findMaxPage">
        SELECT ceil(COUNT(1) / #{size}) FROM board
        <if test=" searchText != null and searchText != '' ">
            WHERE title LIKE '%${searchText}%'
               OR contents LIKE '%${searchText}%'
        </if>
    </select>

    <select id="detail">
        select b.id, b.contents, b.created_at,b.title,u.nm ,b.user_id from board b join user u on u.id=b.user_id where b.id=#{id}
    </select>

    <delete id="delBoard">
        DELETE from board where id=#{id};
    </delete>

    <update id="updated">
        update board set title=#{title}, contents=#{contents} where id=#{id};
    </update>

    <select id="relatedText">
        select distinct title from board where title LIKE concat(#{relatedText},'%') limit 10
    </select>
</mapper>